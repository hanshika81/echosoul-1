# --- Core packages ---
streamlit==1.38.0
openai>=1.3.0
python-dotenv>=1.0.0
cryptography>=42.0.0

# --- Optional / lightweight extras ---
streamlit-webrtc>=0.47.1
nltk>=3.9.1

# --- Notes ---
# Avoid heavy packages (spacy, textblob) to keep build small and fast.
# If you ever add them locally:
# spacy==3.7.2
# textblob==0.18.0
